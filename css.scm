(import (chibi))

(define css
  '((body (padding 0)
          (margin 0))
    (a (text-decoration none))
    (|#links| (font-size |7rem|)
            (text-align right)
            list-style none)))
    (a (@ :hover)

(display css)
